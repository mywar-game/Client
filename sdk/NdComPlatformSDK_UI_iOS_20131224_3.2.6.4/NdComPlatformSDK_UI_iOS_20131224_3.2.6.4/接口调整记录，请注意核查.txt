3.2.6版本
1. 新增NdShowToolBar和NdHideToolBar接口
2. 删除了版本检测更新接口，并在NdInit中增加versionCheckLevel参数用于控制版本检测更新的严格等级
3. NdInitConfigure新增orientation接口，用于表示默认的平台初始化方向。

3.2.5版本
1. 新增NdInit接口，setAppId和setAppKey标记为废弃
2. 新增NdPause接口，用于显示暂停页
3. 接入方需要增加一个格式为91-xxxx(xxxx为软件唯一标识符)的URL Scheme
----------------------------------------------------

3.2.3版本
1、增加kNdCPUserPortraitDidChange消息，用户修改了头像时会发送该消息
2、增加kNdCPUserInfoDidChange消息，用户修改了个人信息时会发送该消息
----------------------------------------------------

3.2.2版本
1、增加软件互推带UI的接口
----------------------------------------------------

3.2.1.2版本
1、恢复切换账号是否需要重启接口shouldRestartApplicationWhenSwitchAccount
----------------------------------------------------

3.2.1版本
1、增加账号管理接口
2、删除切换账号是否需要重启接口shouldRestartApplicationWhenSwitchAccount
----------------------------------------------------

3.2.0版本
  增加软件互推模块的功能接口
----------------------------------------------------

3.1.9版本
  绑定手机号功能接口（查询是否绑定、获取绑定短信验证码，进行账号和手机号的绑定）支持多delegate同时调用，但是接口没有变动。
----------------------------------------------------

3.1.8版本无接口调整
----------------------------------------------------

3.1.7版本无接口调整

---------------------------------------------------------------------

3.1.6版本无接口调整

---------------------------------------------------------------------

3.1.5版本接口调整：

1. 代币充值（NdUniPayForCoin）接口参数调整，增加参数：
     needPayCoins 需支付虚拟币个数
     payDescription 支付描述，发送支付通知时，原样返回给开发者
   具体请参考“91移动开发平台SDK(完整版)参考手册.pdf”   

-----------------------------------------------------------------------------------------

3.1.3版本接口调整：

1. 增加代币充值接口（NdUniPayForCoin）。详情请查看（91移动开发平台SDK（完整版）参考手册）。
2. 调整DEBUG模式（NdSetDebugMode）的调用顺序，需要在调用SDK其他API前调用。详情请查看（91移动开发平台SDK（完整版）参考手册）

-----------------------------------------------------------------------------------------

3.1.2版本接口调整：

1.同步支付、异步支付、虚拟商品购买的结果通知（统一抛kNdCPBuyResultNotification的消息，参数附带信息结构有变）。
  参见《91移动开发平台SDK(完全版)参考手册.pdf》中关于购买结果的监听函数示例代码，目录地址如下：
	三、集成91SDK功能
		11.应用内购买
			1）如何使用同步购买
			2）如何使用异步购买
		12.虚拟商店
			2）进入虚拟商店
			6）购买虚拟商品


2.虚拟商品增加属性。商品计费型信息，商品授权信息增加关于IMEI的信息。
  参见NdVGFeeInfoBase, NdVGAuthInfoBase类定义。

3.NdCheckPaySuccess回调接口更新，增加订单号信息。
  参见NdComPlatform.h头文件接口定义。

4.NdProductIsPayed, NdProductIsExpired回调接口更新，增加是否可在本机使用字段。
  参见NdComPlatform+VirtualGoods.h头文件接口定义。

